Algoritmo TP5_EJERCICIO6
	//RODRIGUEZ ADRIAN
	//TP5_EJERCICIO6
	//6.	Escribe una función que reciba un número como parámetro y devuelva el número que se obtiene al invertir el orden de los dígitos del número original.
	//Por ejemplo, si la función recibe el número 356 devolverá el número 653.
	
	Definir num como entero
	Definir aux Como Entero
	
	num = Aleatorio(101, 999999)
	Escribir "num " num
	Escribir ""
	
	escribir "El numero invertido es " InvertirNumero(num)
	
	
FinAlgoritmo

SubProceso numInvertido <- InvertirNumero(num)

	Definir numInvertido, aux, multiplicador como entero
	multiplicador = 0
	numInvertido = 0
	aux = 0
	
	Mientras num > 0 hacer
		aux = num % 10
		numInvertido = numInvertido * 10
		numInvertido = numInvertido + aux
		num = (num - aux) / 10
		
	FinMientras

FinSubProceso


